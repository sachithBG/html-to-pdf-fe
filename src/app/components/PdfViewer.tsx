// import { Worker, Viewer } from '@react-pdf-viewer/core';
// import dynamic from 'next/dynamic';
// import { pdfjs } from 'react-pdf';

// const PdfViewerComponent = ({ pdfData }: { pdfData: string }) => {
//     return (
//         <Worker workerUrl={`//cdnjs.cloudflare.com/ajax/libs/pdf.js/${pdfjs.version}/pdf.worker.min.js`}>
//             <Viewer fileUrl={`data:application/pdf;base64,${pdfData}`} />
//         </Worker>
//     );
// };

// export default dynamic(() => Promise.resolve(PdfViewerComponent), { ssr: false });
